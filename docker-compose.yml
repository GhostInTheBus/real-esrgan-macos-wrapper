services:
  realesrgan_app:
    build: .
    container_name: realesrgan_video_app
    ports:
      - "8501:8501"
    volumes:
      - ./:/workspace
    ipc: host
    command: >
      bash -lc "
        pip install -e /workspace/Real-ESRGAN
        streamlit run /workspace/streamlit_app.py --server.port=8501 --server.address=0.0.0.0
      "
